'use client'
import Link from 'next/link'
import { SITE_CONFIG } from '@/lib/site-config'
import { useEditableLocalAuthSession } from '@/editable/components/EditableLocalAuthForms'
export function EditableFooter(){const {session,logout}=useEditableLocalAuthSession();const tasks=SITE_CONFIG.tasks.filter(t=>t.enabled&&t.key==='listing');return <footer className="mag-footer"><div className="mag-footer-grid"><div><Link href="/" className="mag-footer-logo">BARRIER<span>POSTS</span></Link><p>A practical directory for finding, comparing and promoting businesses, products and services.</p></div><div><h3>Explore</h3>{tasks.map(t=><Link key={t.key} href={t.route}>{t.label}</Link>)}</div><div><h3>Information</h3><Link href="/about">About Us</Link><Link href="/contact">Contact</Link><Link href="/search">Search Listings</Link></div><div><h3>Account</h3>{session?<><Link href="/create">Create Listing</Link><button onClick={logout}>Logout</button></>:<><Link href="/login">Sign in</Link><Link href="/signup">Sign up</Link></>}</div></div><div className="mag-copyright">© {new Date().getFullYear()} {SITE_CONFIG.name}. All rights reserved.</div></footer>}
